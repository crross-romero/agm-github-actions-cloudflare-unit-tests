name: AGM Workflow

on:
  push: 
    branches:
      - master
      - unit-test

jobs:
  dev-environment:
    runs-on: ubuntu-latest
    environment: develop
    steps:
      - name: Print environment variables
        run: yarn test

  dev-environment:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Print environment variables
        run: yarn test